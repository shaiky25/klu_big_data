---
topic: BDA weekly class deliverables generator
goal: Read topics.txt for the latest dated class topic and generate the three weekly deliverables (PDF, slide deck, posit.cloud R Markdown) following the pattern in the 09/01/2026 model-diagnostics intent doc
updated: 2026-08-29T08:25
---

- (decision by user) Skill name: bda-weekly-deliverables (no bmad- prefix, standalone skill in {project-root}/skills)
- (decision by user) Posit.cloud deliverable format: R Markdown (.Rmd), knittable narrative+code+output, not a plain .R script or .md
- (decision by user) Keynote-import gap on generate_deck.py precedent stays unresolved and out of scope: carry forward only the confirmed-good fixes (Arial font, sldSz@type=screen16x9, docProps/app.xml slide-count patch); do not reopen the paused bisection inside this build
- (note) Source intent doc: _bmad-output/brainstorming/brainstorm-model-diagnostics-validation-r-2026-08-29/brainstorm-intent.md — full voice/register/structure/dataset spec for the 09/01/2026 class, with an explicit Next Step asking for a skill that generalizes this pattern to future weeks by reading topics.txt
- (note) Precedent files: session5/zomato-swiggy-ml-intro.R (commented synthetic-dataset R script, set.seed(42), 4 models), session5/generate_deck.py (python-pptx helper functions + Keynote-safety fixes), session5/ml-masterclass-proof.md->pdf via pandoc (the PDF-document deliverable pattern)
- (event) Wrote scripts/current_topic.py (parses topics.txt, 1-indexed session_number matches confirmed session2..5=entries2..5), scripts/pptx_helpers.py (extracted confirmed-good helpers+Keynote fixes from session5/generate_deck.py), scripts/lint_deck.py (validates slide-count cap, font whitelist, sldSz@type, docProps count)
- (gap) lint_deck.py's font check initially only inspected run.font.name and missed every bad font, since pptx_helpers (and the original generate_deck.py) set font.name on paragraph objects, which python-pptx writes to the paragraph's defRPr, not any run's rPr -- run.font.name stayed None regardless. Fixed by checking both paragraph.font.name and run.font.name, plus table-cell text frames (shape.has_text_frame is False for a table's GraphicFrame). Verified against a deliberately broken deck (Segoe UI, 3 slides, raw prs.save()) and a clean 16-slide deck via .venv/bin/python3.
- (event) Added scripts/tests/ (test_current_topic.py, test_pptx_helpers.py, test_lint_deck.py, 11 cases, all pass via .venv/bin/python3) and PEP723 dependency blocks + sys.exit() on the two CLI scripts; scan-scripts.py now status=pass
- (decision) Left 3 medium/low scan-scripts findings on pptx_helpers.py unaddressed (no argparse/json.dumps/sys.exit): it's an imported helper library, not a standalone CLI -- forcing that shape would be unneeded structure, not a real defect
- (decision) scan-path-standards.py 'high' finding on .memlog.md at skill root is not being fixed: build-process.md explicitly and repeatedly writes the memlog to {target-skill-path}/.memlog.md, and the builder's own SKILL.md resume-detection step globs that exact path. Moving it to references/.memlog.md would silently break resume for this skill. Treating as an accepted scanner false-positive, surfaced to the user rather than silently fixed or silently ignored
- (decision by user) customize.toml declined -- ships fixed, no resolver step, hardcoded paths throughout
- (event) Dogfood run: generated real session6 deliverables for 09/01/2026 (model diagnostics) using the built skill's own instructions -- Rmd (knits clean, 33 chunks, numbers verified against a standalone exploration script before writing narration), 16-slide deck via pptx_helpers (lint_deck.py: ok, no issues), PDF via pandoc
- (gap) SKILL.md's documented pandoc command failed on real content: default pdf-engine can't handle unicode (>=, superscript 2, em dash) that this voice/content style naturally produces ('Unicode character not set up for use with LaTeX'). Fixed by adding --pdf-engine=xelatex to the documented command in SKILL.md before this was discovered by a future run.
- (note) Deck slide count needed real expansion from a first draft of 9 slides to 16 to satisfy the 15-20 range -- done via genuine content splits (methodology slide, split fit-vs-reading slides, takeaways, closing) not padding; logged so a future week's first pass budgets for ~15-16 slides across 4 models + framing + close, not ~9
- (event) analyze: grade good, 0 critical / 2 high / 5 medium / 2 low, report .analysis/2026-08-29-0817/skill-analysis-report.html
- (event) Applied all 9 Analyze findings: soft-gate checkpoint before building off a self-derived brief + headless fallback for the too-raw case (enhancement-1, enhancement-3); scripted brainstorm-intent.md date matching into current_topic.py's new content_brief_path field (determinism-1); added scripts/lint_rmd.py (set.seed(42) + no-external-reads checks) wired into Build and Validate (determinism-2); added PDF check to Validate mode (architecture-1); Known-gap section now instructs flagging residual Keynote risk at every handoff (architecture-2); restored generic {skill-name} resolution-rule token (architecture-3); dropped duplicate no-medical-framing clause (leanness-1); added a pre-handoff self-review pass against the Voice/register rules (enhancement-2). Added 2 new tests for content_brief_path, 5 new tests for lint_rmd.py -- 18 total, all pass. Re-ran quick_validate/scan-path-standards/scan-scripts clean (only accepted exceptions remain: .memlog.md at root, pptx_helpers.py library-not-CLI findings).
